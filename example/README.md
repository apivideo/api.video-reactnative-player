# @api.video/react-native-player sample application

Simple application to demonstrate how to use the api.video's React Native video player. 

## Installation

From the root folder of the repo:

### Build the component
```bash
npm install
npm run build
```

### Build & run the example application

```bach
cd example/
yarn
yarn run android # or: yarn run ios
```